{
  "name": "somedom",
  "version": "0.1.5",
  "description": "Building blocks for the DOM.",
  "main": "dist/somedom.js",
  "module": "dist/somedom.es.js",
  "browser": "dist/somedom.umd.js",
  "files": [
    "dist/*.*",
    "ssr.js"
  ],
  "scripts": {
    "dev": "rollup -c -w",
    "build": "rollup -c",
    "watch": "npm test -- -w",
    "serve": "sirv start docs --dev --quiet --single --port 8080",
    "test": "npm run test:e2e -- ${BROWSER:-chrome:headless} -a 'npm run serve' tests/e2e/cases",
    "test:ci": "npm run coverage && npm run report -- -r lcov && npm run test -- --color",
    "test:unit": "NODE_ENV=test _mocha --exit --colors --recursive -r esm -bR spec tests/unit",
    "coverage": "nyc --silent -x '**/fixtures/**' -x '**/*.test.js' npm run test:unit",
    "coverage:unit": "npm run coverage -- npm run test:unit",
    "report": "nyc report",
    "lint": "eslint src tests",
    "pretest": "npm run lint",
    "prepublishOnly": "NODE_ENV=production npm run build",
    "test:e2e": "BASE_URL=http://localhost:8080 testcafe --color"
  },
  "author": "Alvaro Cabrera <pateketrueke@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "chai": "^4.1.2",
    "cookie-signature": ">=1.0.4",
    "ejs": ">=2.5.5",
    "eslint": "^6.8.0",
    "eslint-config-airbnb-base": "^14.0.0",
    "eslint-plugin-import": "^2.14.0",
    "esm": ">=3.1.0",
    "express": ">=3.11.0",
    "fresh": ">=0.5.2",
    "js-yaml": ">=3.13.1",
    "kind-of": ">=6.0.3",
    "lodash": ">=4.17.13",
    "mime": ">=1.4.1",
    "minimist": ">=1.2.3",
    "mocha": "^7.1.1",
    "moment": ">=2.19.3",
    "nyc": "^15.0.1",
    "qs": ">=1.0.0",
    "rollup": "^2.6.1",
    "rollup-plugin-commonjs": "^10.1.0",
    "rollup-plugin-node-resolve": "^5.2.0",
    "rollup-plugin-terser": "^5.1.3",
    "send": ">=0.11.1",
    "serialize-javascript": ">=2.1.1",
    "set-value": ">=2.0.1",
    "sirv-cli": "^0.4.4",
    "testcafe": "^1.8.4",
    "testdouble": "^3.11.0",
    "tough-cookie": ">=2.3.3",
    "uglify-js": ">=2.6.0",
    "useragent": ">=2.1.13",
    "ws": ">=1.1.5"
  }
}
